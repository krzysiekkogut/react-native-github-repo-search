# react-native-github-repo-search
React Native app - searching GitHub repositories

## Prerequisites
* Follow instruction on https://facebook.github.io/react-native/docs/getting-started.html

## To run the app:
1. `yarn install`
2. `react-native run-android` with Android emulator or device connected or `react-native run-ios` (only macOS)
